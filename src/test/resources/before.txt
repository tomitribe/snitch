    // void return type

    ${keywords} void voidMethodTime${number}(${parameters}) throws IllegalStateException {
    }

    // object or primitive return type

    ${keywords} ${type} ${typeSimpleName}MethodTime${number}(${parameters}) throws IllegalStateException {
        throw new UnsupportedOperationException();
    }

    // array object or primitive return type

    ${keywords} ${type}[] ${typeSimpleName}ArrayMethodTime${number}(${parameters}) throws IllegalStateException {
        throw new UnsupportedOperationException();
    }
